# Notebook usage

- GitHub may show the notebook source without executed outputs.
- Use the Colab link and click `Runtime → Run all`.
- After running, save or export the executed notebook if needed for submission.
